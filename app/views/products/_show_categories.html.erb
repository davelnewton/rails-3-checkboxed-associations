<% if not @product.categories.empty? %>
  <strong><%= @product.categories.size == 1 ? "Category" : "Categories" %></strong>
  <ul>
    <% @product.categories.each do |cat| %>
      <li><%= cat.name %></li>
    <% end %>
  </ul>
<% end %>